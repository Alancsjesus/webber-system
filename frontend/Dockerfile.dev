FROM node:20

WORKDIR /app

# Desabilita verificação SSL estrita (necessário em redes com inspeção TLS corporativa)
ENV NPM_CONFIG_STRICT_SSL=false

COPY . .
RUN npm install

EXPOSE 5173
CMD ["node", "node_modules/vite/bin/vite.js", "--host", "--port", "5173"]
